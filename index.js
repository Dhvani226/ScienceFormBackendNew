import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Ensure 'app' is correctly named and the path is valid

ReactDOM.render(<App />, document.getElementById('root'));
